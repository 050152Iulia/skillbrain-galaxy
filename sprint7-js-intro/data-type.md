# Data Types:

- `string`: "Ana", "A", "", `Mere`,
Este un text, poate fi si o litera, sau mai multe sau niciunul
Orice cuvant care are ghilomelele ("") sau apostroafe (``) este considerat string
Recomandam sa folosim ghilimelele  in loc de apostrof. exceptie facem cand ghilimelele fac parte din text:
ex: `const a = `El as psus: "Mergem acum? "`
ex `

`Number` : este un numar, poate fi real si complex chiar imaginar.
numere intregi: `integer`: 1, 2, 3, etc
reale
- cu virgula 23.5, 7.25, 
- forma stiintifica 3a+5, 2e-20
- imaginare; -Infinity, Infinity
- nu numar; Not a number NaN

boolean - tip de date doar cu doua valori
undifined - tip de date aparte care nu are valoare - apare atunci cand variabila nu este declarata inca(ex `let a`)

`Symbol` - tip de date simbolic, care nu poate fi converti la un numar si nu se pot face operatii(nu il folosim)
se defineste cu un parametry(ex: `const a = Symbol("Ana")`)







